import { ProductConsumption } from './product-consumption';

describe('ProductConsumption', () => {
  it('should create an instance', () => {
    expect(new ProductConsumption()).toBeTruthy();
  });
});
