import { OrderRequestModel } from './order-request-model';

describe('OrderRequestModel', () => {
  it('should create an instance', () => {
    expect(new OrderRequestModel()).toBeTruthy();
  });
});
