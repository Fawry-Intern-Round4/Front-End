import { TransactionRequestModel } from './transaction-request-model';

describe('TransactionRequestModel', () => {
  it('should create an instance', () => {
    expect(new TransactionRequestModel()).toBeTruthy();
  });
});
